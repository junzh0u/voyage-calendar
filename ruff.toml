target-version = "py313"

[lint]
# The defaults, spelled out: the file predates the modern rule set and its
# shell templates read best as .format calls; naive wall clocks are by design.
select = ["E4", "E7", "E9", "F"]
